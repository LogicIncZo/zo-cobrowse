# 0.2.7 Release Slate — Polish & Daily-Use Round (design)

**Date:** 2026-08-30 · **Status:** approved in-session · **Milestone:** 0.2.7 (file issues at implementation start)

## Goal

A second consecutive hardening round, this time paired with user-visible daily-use wins. 0.2.6 closed observation-driven bugs and built the measurement layer (coverage, perf baseline, prompt-token audit, i18n scaffolding); 0.2.7 *spends* that layer: it executes the follow-ups the 0.2.6 audits explicitly named, and adds three small features that remove daily friction (chat export, stale-build guard, streaming-reasoning probe).

Deliberately **not** a feature-slate round: the Tier-1 carry-over (#19 model picker, #10 cross-tab actions, #29 watch & scheduled tasks) stays parked for 0.3.0 planning, as does the i18n string migration (#68 round 2). Prerequisite: **v0.2.6 tags first** (owner testing pending); 0.2.7 work lands on `dev` on top of the merged 0.2.6 slate.

## Lanes

### Lane A — prompt/token efficiency (audit follow-up, #71)

| Order | Item | Summary | Key decisions |
|---|---|---|---|
| 1 | Prompt-bloat trims | Per the audit's finding #2: persona, mode `systemPrompt`, and mode `instructions` each restate the no-submit (#26) / no-secrets rules — on tier-0 turns system+tail are ~90% of the prompt. Consolidate into ONE shared rule block composed once in `lib/prompt.js` (`_compose`-tagged so string + structured views stay in sync); strip the duplicated sentences from built-in modes' fields where the shared block already covers them. | Shared block lives in `lib/prompt.js` (single source), not `modes.js` — modes keep per-mode fields but their text shrinks. User sparse overrides unaffected (they replace, never accumulate). Before/after totals appended to `docs/qa/prompt-bloat-audit.md`; evals green (`bun run evals:live` refresh in same PR, case ids stable). |

### Lane B — perf (audit follow-up, #67 — evidence rule applies)

The baseline is explicit: prompt-side compute is microseconds (off the table). The named cost centers are environmental, and two of the three need owner-run diagnostics to justify work. Scope is therefore **analysis-first with pre-named candidates**:

| Order | Item | Summary | Key decisions |
|---|---|---|---|
| 2a | Diagnostics analysis | Owner runs the on-device recipe (debug mode ON, a real session, Copy diagnostics) and shares the export; we analyze first-turn vs warm-turn `capture` (cold start + IPC), `stream:done` distribution, and hops/turn. | If no export is available yet, this item blocks (documented) — no intuition-driven work in its place. |
| 2b | SW cold-start trim (pre-named candidate) | Audit the static import graph the service worker evaluates at cold start; move non-critical modules (e.g. MCP transport, vision catalog fetch paths) to lazy dynamic `import()`. Gated on a committed micro-bench of import-graph eval cost (bun-timed) showing a meaningful saving; the owner's diagnostics export (2a) is the acceptance cross-check. | Only modules not needed on the first message hop may defer. `lib/` pure modules stay static (they're microseconds). |
| 2c | Chunk-render ratchet | Only if exports show long gaps between chunk arrival and paint: batch panel-side DOM appends per animation frame. | Strictly conditional on 2a evidence; otherwise closes as a documented no-op. |

### Lane C — fixes & hygiene (QA-report + chores)

| Order | Item | Summary | Key decisions |
|---|---|---|---|
| 3 | Test Connection honors configured endpoint (QA finding B, P3) | `testConnection()` hardcodes `api.zo.computer`, ignoring the configured `zoApiUrl` — self-hosted/custom endpoints can never pass Settings' test. Use the configured endpoint. | Integration test asserts the configured URL is called; error text keeps quoting the actual URL tried. |
| 4 | Reconnecting banner (QA finding D, P4) | The `Reconnecting…` banner is dead code on the transient-error path — `isRetriableStreamError()` retries and emits `STREAM_RECONNECT` without ever showing it. | Decide by inspection: surface the banner where `STREAM_RECONNECT` fires (preferred — it's honest UX) or delete the dead markup. Tiny change + test. |
| 5 | Version/docs hygiene | Root `package.json` says `0.1.0` (stale); `docs/roadmap.md` is a stale snapshot (claims v0.2.0 current, 977 tests). Sweep + fix. | `package.json` version syncs to the manifest version at release prep (both read `0.2.7` when v0.2.7 tags) — one decision, written down here. `docs/roadmap.md` reduced to a pointer at BACKLOG.md (the authoritative doc) to stop the drift. Dead-code sweep (finding D rides item 4). |

### Lane D — daily-use UX (new, small)

| Order | Item | Summary | Key decisions |
|---|---|---|---|
| 6 | Chat export | Export a conversation as Markdown from the history view (row action). Serializer is a pure `extension/lib/export.js` ES module (no `chrome.*`/DOM deps) → unit-tested + Zod schema per repo pattern; sidepanel triggers a `Blob` + `a[download]`. | Format: title header, role-labeled turns (`**You**` / **Zo** + mode/model footer data + relative timestamps), reasoning as `> 💭` blockquote when present, context-tier chip noted on assistant turns; tool-trace internals omitted. Filename `zo-chat-<slug>-<YYYYMMDD>.md`. Pure sidepanel + lib — no new message types. |
| 7 | Stale-build guard | Kills the recurring "still broken after fix" loop (stale content script in open tabs): on `chrome.runtime.onInstalled` (reason `update`) the background re-injects `content.js` into eligible open `http(s)` tabs, and the sidepanel shows a one-time dismissible "Extension updated" banner (flag in `storage.session`). | **Prerequisite: `content.js` must be injection-idempotent** (guard flag so a second injection doesn't double-bind listeners) — verified/added first, with a test. Excluded: `chrome://` pages, the extension's own pages, tabs without host permission (none today, `<all_urls>`). |
| 8 | Streaming-reasoning probe (timeboxed: one live-probe session + one PR max) | Reasoning today arrives only in the final `STREAM_DONE` payload. Use the existing `emitStreamDiagnostic`/`streamShape` machinery + a live probe (like the #25 vision probe) against real Zo SSE to answer: does Zo emit incremental reasoning events? | **Yes** → implement inline incremental reasoning rendering (reuses `addReasoningBubble`; chunks append into the live trace header). **No** → record the finding in QA_REPORT + AGENTS.md streaming section and close. No speculative implementation either way. Probe script committed under `scripts/` like its predecessors. |

## Build order

Lane C items (3, 4, 5) first — small, independent, warm-up. Then Lane D (6, 7, 8; 7's idempotency prerequisite first). Lane A (1) when evals refresh budget is available (needs `ZO_API_KEY`). Lane B (2a–2c) interleaves: 2a needs the owner's diagnostics export, 2b can proceed on its micro-bench.

## Cross-cutting rules (inherited from 0.2.6)

1. **Prompt changes** stale the evals cache → `bun run evals:live` refresh rides the same PR; case ids stable (stream-replay fixtures route on them).
2. **Tests first where pure logic**: `lib/` halves get schemas + unit tests before wiring.
3. **Evidence rule**: perf work quotes before/after numbers (baseline instrument or committed micro-bench); prompt work quotes before/after token totals. No intuition-driven changes.
4. Git-flow: one `fix/*`/`feat/*`/`chore/*` branch per item, PR to `dev`; `main` promotion + tag `v0.2.7` is the deliberate release step. Issues filed per item (#91+) at implementation start.

## Out of scope (0.2.7)

- Tier-1 carry-over: #19 model picker, #10 cross-tab actions, #29 watch & scheduled tasks → 0.3.0 planning.
- i18n string migration + second locale (#68 round 2).
- All current 0.3.0 milestone items (#46–#54), Web Store (#11), shared sessions (#15).
- `sidepanel.js` module split (revisit as its own low-risk round).

## Release mechanics

`v0.2.6` tags first (owner testing pending) — 0.2.7 accumulates on `dev` above it. Changelog `[Unreleased]` accumulates the lane merges (the 0.2.6 slate moves under its `0.2.6` heading at tag time). BACKLOG.md's milestone section tracks status. Manifest bumps to `0.2.7` at release prep; dev→main PR gated by CI + `drift` job; manual panel checklist before promotion.
