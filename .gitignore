node_modules/
*.log
.env
.DS_Store
dist/
coverage/

# Design docs, brainstorming, and planning (not production code)
brainstorming/

# Skill runtime symlink (created by install)
skill/installed

# Local package output (bun run package). The bundled zip is never committed.
zo-cobrowse.zip

# VitePress docs site build/cache output (npm run docs:build). Never committed.
docs/.vitepress/dist/
docs/.vitepress/cache/

# bun.lock is intentionally tracked for deterministic CI installs.
database/

# Local tool/agent artifacts — never committed
.mimosa/
.zcode/
# Playwright e2e output (reports, traces, test profiles)
e2e/report/
e2e/results/
e2e/qa-matrix/report/
e2e/qa-matrix/results/
playwright-report/
# Demo recording output (ZO_DEMO=1 run of e2e/demo-open-all.spec.ts)
e2e/demo-video/
demo/

# Vision probe artifact (regenerated per run by tests/test-prompts/probe-vision.ts)
tests/test-prompts/fixtures/vision-probe-image.jpeg

# Swamp model instances (machine-local; the committed source of truth for the
# factory definition is scripts/swamp/factory.yaml — see docs/swamp-factory.md)
models/

# BEGIN swamp managed section - DO NOT EDIT

# Runtime data (not needed in version control)
.swamp/

# Local extension sources (developer-specific, not shared)
.swamp-sources.yaml

# Claude Code configuration (managed by swamp)
.claude/worktrees/
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/scheduled_tasks.json
# END swamp managed section
